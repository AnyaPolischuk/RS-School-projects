import { startCarEngine } from './app';

// export const animationCar = (id: number) => {
//     startCarEngine(id).then((res) => {
//         const duration = res.distance / res.velocity;
//         const distance = res.distance;
//         let startAnimation = null;
//         // console.log('продолжительность', duration);
//         // console.log('расстояние', distance);

//         requestAnimationFrame(function measure(time: number)) {
//             if (!startAnimation) {
//                 startAnimation = time;
//             }

//             const progress = (time - startAnimation) / duration;
//             const translate = progress - distance;

//             carSVG

//         }
//     });
// };
